title: Обучение
description: Впервые знакомлюсь с данной тематикой
template:
  repo: obuchenie-template
  name: obuchenie
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
